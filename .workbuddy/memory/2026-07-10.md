# 2026-07-10 工作日志

## 后端容器被关闭导致后台无法显示（已修复）
- **现象**：用户反馈"后台无法显示"。检查发现 4 个容器里 `pet-feeding-backend` 不在运行（只有 frontend/nginx/mysql）。
- **根因**：后端容器 `fb455603c579_pet-feeding-backend` 在 14:27 收到 SIGTERM 优雅退出（退出码 143，日志显示正常走完 Spring shutdown hook，非崩溃、非 OOM；服务器内存充足 2.5G 可用）。导致 nginx 代理 `/api` 到后端时 `No route to host`，所有 API 返回 502，前端页面加载不出数据。
- **旧容器名前缀说明**：`fb455603c579_pet-feeding-backend` 是旧 compose 项目名的 hash 前缀，当前 compose 项目容器名应为 `pet-feeding-backend`。recreate 时因旧容器名冲突报错，需先 `docker rm` 旧容器再 `up`。
- **修复**：`docker rm fb455603c579_pet-feeding-backend` → `docker compose up -d backend`，新容器名 `pet-feeding-backend` 正常启动。
- **验证**：登录 `/api/user/login` 返回 200+Token；`/api/pet/all`、`/api/dashboard/stats` 均 200 且中文数据正常。后台恢复显示。
- **后续关注**：后端是被 SIGTERM 停掉的（非崩溃），需留意是否有人/脚本再次 stop。若复发，查 `docker events` 或加进程监控。重启策略已是 `unless-stopped`（手动 stop 不会自动拉起）。

## 环境信息（再次确认）
- 后端日志偶现 "Using generated security password ... for development use only"，说明 prod profile 下仍会生成 Spring Security 默认 user 账号，但不影响自定义 JWT 登录，暂未处理。

## 代码同步核查（用户问"是不是代码更新了没上传"）
- **结论**：生产相关代码已全部同步到服务器，后台故障（容器被停）与代码未上传无关。
- 逐项对比（本地 working tree vs 服务器 /home/ubuntu/docker-deploy/pet-feeding-admin-docker/）：
  - `backend/.../MiniAppAuthController.java`（+短信验证码接口）：服务器已含新代码，IDENTICAL ✅
  - `backend/.../MiniAppUserService.java`（+验证码发送/校验）：服务器已含新代码，IDENTICAL ✅
  - `backend/.../application-prod.yml`：本地 vs 服务器 IDENTICAL ✅
  - `docker-compose.yml`：本地工作区原本**缺失**（服务器是 07-09 加固版：mysql 绑 127.0.0.1 + 强密码）。已 scp 拉回本地 `D:\Workspace\pet-feeding-admin\docker-compose.yml` 备份 ✅
  - `application-dev.yml`：本地有改动（微信小程序 app-id 改为 wx1ad1b47d97fb0e04 + 真实 app-secret）。仅影响 dev profile，生产用 prod 不受影响；服务器 dev 配置未单独核查。
- **真实隐患**：本地 git 有大量 unstaged 改动（两个 Java 文件 + dev/prod yml），但 git HEAD 仍是旧版（无验证码功能）。即 **git 仓库代码落后于实际部署代码**。若服务器镜像丢失需重建、且用 git 代码 build，会 build 出旧代码。建议把本地改动提交到 git。
- 当前运行的后端容器是 14:30 新建的，build 时用的服务器源码已含最新改动，故容器内代码为最新。

## 整体重新部署（用户要求"把本地最新上传重新部署"）
- 删除了上次被中断的构建残留容器 `busy_taussig`/`cool_maxwell`，以及旧的后台重建任务(pid 1691141)。
- 重新 tar 同步本地 backend/frontend 源码到服务器（保留服务器 Dockerfile，不用 --delete）。
- `docker compose down` 停止删除全部旧容器（保留 mysql_data 数据卷）→ `setsid` 服务器本地后台 `docker compose up -d --build`（日志 /tmp/redeploy2.log，BUILD_DONE 成功，4 容器全 Up）。
- 清理无用镜像：`nginx:1.27-alpine`、`pet-feeding-admin-docker_backend`(下划线版)、`pet-feeding-admin-docker_frontend`(下划线版)，释放约 620MB。保留运行/构建必需镜像。

## 客户管理加性别字段（男/女）— 已实现并部署
- 用户确认：客户管理需要分男/女，选"只加男/女两选项"。
- **后端改动**：
  - `User.java` 实体加 `gender` 字段；`RegisterDTO.java` 加 `gender`。
  - 新增更新接口 `PUT /api/user/{id}`，`UserService.updateProfile(id, Map)` 支持更新 phone/email/gender。
  - `init_prod_db.sql` 的 users 表加 `gender VARCHAR(10)` 列。
- **数据库**：线上执行 `ALTER TABLE users ADD COLUMN gender VARCHAR(10) AFTER avatar;`（GENDER_COLUMN_ADDED）。
- **前端改动**：
  - `api.js` 加 `userApi.update(id, data)`。
  - `UserList.vue` 列表加"性别"列（👨男/👩女/未填显示 -）；新增"编辑"按钮弹窗（性别下拉男/女 + 手机号 + 邮箱），保存调 update 接口。
- **重新部署**：再次 tar 同步 → `setsid docker compose up -d --build`（日志 /tmp/redeploy3.log，BUILD_DONE 成功）。
- **验证**：`PUT /api/user/2 {"gender":"男"}` 返回 200，列表查得 zhangsan gender=男（已恢复 null）；前端产物 `UserList-Cro7q3M2.js` 含"性别"，部署生效。4 容器 running。

## 性别功能增强（新增客户 + 性别筛选 + 仪表盘性别分布）— 已实现并部署
- 用户说"加上吧"，把之前提的三项全做了：新增客户弹窗、按性别筛选、仪表盘客户性别分布。
- **后端改动**：
  - `UserController.list` 增加 `gender` 参数；`UserService.listByRole(role, gender)` 按性别过滤（LambdaQueryWrapper.eq Gender）。
  - `DashboardController.stats` 增加 `ownerGenderBreakdown`（男/女/未填，基于 role=OWNER 统计；未填=总数-男-女）。
- **前端改动**：
  - `api.js`：`listByRole(role, gender)` 支持传 gender。
  - `UserList.vue`：用 PageTable 的 `#header-actions` 插槽加"+ 新增客户"按钮（调用 `POST /api/user/register`，表单含 用户名/密码/角色/性别/手机号/邮箱，密码默认 123456 可改）；用 `#filters` 插槽加性别筛选下拉（全部/男/女），change 时重新拉列表。
  - `Dashboard.vue`：charts-row 改为三列，新增"客户性别分布"甜甜圈图（蓝=男/粉=女/灰=未填），接入 `ownerGenderBreakdown`；新增 genderCanvas/genderChart 渲染与销毁。
- **重新部署**：tar 同步 → `setsid docker compose up -d --build`（日志 /tmp/redeploy4.log，BUILD_DONE 成功，4 容器全 Up）。
- **验证**：
  - 性别筛选 `/api/user/list?role=OWNER&gender=男` 返回 1 条（male owners:1）✅
  - 仪表盘 `ownerGenderBreakdown: {'男':1,'女':0,'未填':7}` ✅
  - 新增客户：`POST /api/user/register` 创建 test_gender_001(gender=女) → HTTP 200 → 清理后女客户数回 0 ✅
  - 前端 `UserList-BNaG7Dxk.js` 含"新增客户"、`Dashboard-CrR94krc.js` 含"客户性别分布"，部署生效 ✅

## 后端优化 & 前后端闭环审计（用户问"是否优化了、是否联动形成完美闭环"）
- **结论**：联动 ✅ 全打通（前端 10 页面所有 API 调用 100% 有后端实现，零 404 断点）；功能闭环 ✅ 成立（Admin 管理端 + 小程序端各自闭环，创建类操作职责分离，非缺陷）。
- **后端优化待办（写进 backend-frontend-audit.md）**：
  - P0-1：`JwtAuthFilter.java:42` Authorities 传 `Collections.emptyList()` → 角色权限未生效，任何登录用户可调用任意管理接口（越权防护地基缺失）。
  - P0-2：`CorsConfig.java:18-21` `addAllowedOriginPattern("*")`+`setAllowCredentials(true)` 过宽（带凭据通配源，规范非法；Admin 同源本不需 CORS）。
  - P0-3：`SecurityConfig.java` 未放行 `HttpMethod.OPTIONS` 预检（依赖 CorsFilter 兜底）。
  - P1-4：列表接口全量返回，无分页。
  - P1-5：`updateProfile`/`updateStatus` 用 `Map<String,String>` 当 DTO（无校验）。
  - P1-6：`DataSourceInitializer` `@Profile("!prod")` → prod 依赖手动建表（数据卷丢失需手工补）。
  - P1-7：prod 仍打印 Spring Security dev 默认密码（噪音）。
- 报告产出：`D:\Workspace\pet-feeding-admin\backend-frontend-audit.md`。待用户确认是否执行优化。

## 前端 issue #1：宠物平台审核流程（需求拆解，待实现）
- **来源**：https://github.com/mayunfei520/pet-feeding-admin/issues/1 前端提的【需求】添加宠物走平台审核流程（待审核→通过/驳回）。
- **目标**：小程序提交宠物后不直接生效，进入 PENDING；管理员后台审核通过(APPROVED)才对外可见/可预约；驳回(REJECTED)告知原因。
- **后端需做（已核对当前代码）**：
  1. `Pet.java` 加 `status`(PENDING/APPROVED/REJECTED) + `rejectReason`(≤200)；`schema.sql` pets 表加 `status` 默认 PENDING + `reject_reason` 列（线上需 ALTER TABLE）。
  2. `MiniAppPetController.add()`：save 前强制 `pet.setStatus(PENDING)`，忽略客户端传入 status（防伪造）。
  3. 可见性：`/api/miniapp/pets`(我的宠物) 返回全部(含PENDING) 现状已满足；`OrderServiceImpl.createOrder` 需校验宠物 status==APPROVED 否则拒绝（预约防护）。
  4. 现有 `PetController`(/api/pet/all) 无角色限制，OWNER/FEEDER 登录可见全部宠物(含待审核)——违反"对他人不可见"，需收进 ADMIN 或过滤 APPROVED。
  5. 新建 `AdminPetController`(/api/admin/pets，仅 ADMIN)：GET 审核队列(?status=PENDING)、POST /{id}/approve、POST /{id}/reject{reason}；非ADMIN→403，重复审核→400；SecurityConfig 加 `/api/admin/**` 到 ADMIN。
  6. 通知主人（issue 第5点可选）——建议先不做。
- **前端自处理**：提交后提示"等待审核"、列表状态徽标、PENDING 禁用"去预约喂养"。
- **待用户确认**：是否现在直接实现并部署验证。
