# 2026-07-06

## 响应式布局修复

- **Layout.vue** 侧边栏之前固定 240px 无媒体查询，窄屏严重挤压内容区
  - 新增汉堡按钮（≤768px 显示），侧边栏变为滑入抽屉 + 半透明遮罩
  - 平板模式（≤1024px）侧边栏收窄为 72px 图标条
  - 路由切换自动关闭移动端抽屉
- **Dashboard.vue** `max-width: 1260px` 限制去掉，改为 `width: 100%` 撑满容器
- 其他页面（FeederList/ReviewManage）的 max-width 均为单元格截断用途，无需改动
- 前端构建通过，后端用 Maven 重新打包（PowerShell 运行 mvn 成功，Git Bash 下 mvn 报 classworlds 错误）

## 环境备注

- 系统默认 Java 是 1.8.0_491（D:\Package\Java\jdk1.8.0_491），Spring Boot 2.7.18 兼容
- Maven 路径：D:\Package\apache-maven-3.9.11，Git Bash 下直接运行报错，需用 PowerShell 运行
- 后端 jar 打包后 43MB，启动约 4 秒，prod profile 直连 101.42.24.114 MySQL
