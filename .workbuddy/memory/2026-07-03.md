# 2026-07-03

## 喂养员管理 + 订单管理页面样式统一

- 重写 FeederList.vue：丢弃原始 `<table>`，改用 PageTable 内置表格，斑马纹/hover/sticky 表头统一
- Tab 按钮去 emoji，改用品牌色分段按钮 + 数字徽章
- OrderList.vue：移除死代码样式，select 用全局 `.select` 类
- style.css：将 `.select` / `.input` 基础样式提升到全局，避免各页面重复定义
- PageTable.vue：移除已提升到全局的冗余 scoped 样式

## 表格操作按钮样式修复

- 用户反馈表格中按钮显示为原生样式
- 原因：.btn / .tag 等样式放在 PageTable scoped 中，无法作用到父组件传入的 slot 内容
- 修复：将 .btn / .tag / .stars 等可复用组件样式从 PageTable.vue 移到全局 style.css

## 支付记录页面修复

- PaymentList.vue 状态列改用 slot + `<span class="tag">` 渲染，解决 HTML 字符串被当文本输出的问题
- 后端 DataSourceInitializer.java 种子数据补充 transaction_id
- 当前运行中的后端无法停止/重建（Java/Maven 被沙箱拦截），所以现有演示数据里交易号仍为空；重启后端重新初始化数据后才会显示

## 仪表盘全面升级

- DashboardController 后端重构：接入 PaymentService 统计真实营收（原来 revenue 写死 BigDecimal.ZERO）
- 新增 API 返回：订单状态分布（PENDING/ACCEPTED/IN_PROGRESS/COMPLETED/CANCELLED 各数量）
- 新增 API 返回：近 7 天每日订单趋势数据
- 新增 API 返回：最近 5 条订单 + 最近 5 条注册用户
- Dashboard.vue 前端重写：
  - 修复 6 个统计卡片 SVG 图标（原来用无效的 component :is 引用，图标不渲染）
  - 新增 Chart.js 图表：订单趋势折线图（带渐变填充）+ 订单状态环形饼图
  - 新增左右两栏动态列表：最近订单（状态色点 + 金额）+ 最近注册用户
  - 快捷操作换上差异化图标（审核 ✓、物流车 🚚、用户群 👥）
- pom.xml 补充 mysql-connector-java 显式版本号（8.0.33），修复 Maven 编译报错
- 新增 chart.js npm 依赖
- User 实体无 nickname 字段，dashboard 适配用 username 替代
- 需要重启后端才能看到新仪表盘数据

## schema.sql BOM 乱码修复

- schema.sql 开头存在 UTF-8 BOM（字节序标记 0xEFBBBF），MySQL 执行时会导致第一行无法正常解析
- 已移除 BOM，文件现在干净无乱码

## 仪表盘全面重做 v2

- 用户对 v1 仪表盘不满意，全面重写 Dashboard.vue
- 核心改进：
  - **骨架屏**：3 种状态（加载 shimmer / 错误重试 / 正常），不再直接显示 0
  - **数字滚动动画**：6 个 KPI 卡（easeOutCubic 缓动，1.4s），数值从 0 动态滚动到实际值
  - **KPI 卡片重设计**：3 列网格、48px 渐变图标、待处理订单带脉冲红点警示
  - **图表空状态**：无数据时显示占位 SVG + 提示文字，不再空白
  - **去掉重复标题**：Dashboard 不再自建标题栏（Layout 已有"仪表盘"标题）
  - **最近订单/用户**：状态标签改用彩色 tag pill、价格格式化千分位、相对时间
  - **快捷入口**：渐变图标、hover 浮起动效
- 前端构建通过（vite build 0 错误），Dashboard JS 214KB / CSS 8KB

## 连接服务器 MySQL 并修复数据

- 用 PyMySQL 直连 101.42.24.114:3306，root 只有读权限，业务账号 petfeeder 有读写权限
- 修复 feeders.real_name：原始数据 hex=e69d8ee6988e 本身就是正确的 UTF-8 "李明"，终端显示乱码是 Git Bash 渲染问题，数据库内数据无误
- 支付记录补上 transaction_id: `WX2024010112345678`
- admin 密码 BCrypt hash 验证通过 (123456)
- 数据库状�：7 表完整，7 用户、3 订单、1 支付、1 评价，全部就绪
